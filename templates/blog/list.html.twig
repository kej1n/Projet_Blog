{% extends "base.html.twig" %}
{% block body %}
    <h1>Page number {{ page }}.</h1>
    <a href="{{ path('app_index') }}">Retour à l'accueil</a>
    <br>
    <a href="{{ path('category_show', {'categoryName': 'javascript'}) }}">Voir les categories</a>
    <br>
    <a href="{{ path('blog_index') }}">Afficher les Articles</a>
    <br>
    <a href="{{ path('blog_show', { 'slug': 'javascript-vs-php' }) }}">Afficher un slug</a>
    <br>
    <br>
    <a href="{{ path('blog_list', {'page':1}) }}">Afficher la page 1</a>
    <a href="{{ path('blog_list', {'page':2}) }}">Afficher la page 2</a>
    <a href="{{ path('blog_list', {'page':3}) }}">Afficher la page 3</a>
{% endblock %}